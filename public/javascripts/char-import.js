function importChar(region, char, realm) {
    var url = region + 'battle.net' +'/api/wow/character' + realm + '/' + char;
    var statsUrl = url + '?fileds=stats';
    
    // So we should be sent data with all the char dataz heh
    
}